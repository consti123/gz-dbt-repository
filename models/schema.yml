# schema.yml
version: 2

sources:
  - name: raw
    database: my-own-project-475410
    schema: gz_raw_data
    loader: bigquery
    tables:
      - name: sales
        identifier: raw_gz_sales

      - name: product
        identifier: raw_gz_product
      - name: ship
        identifier: raw_gz_ship
